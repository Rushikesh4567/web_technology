# Response object:


common response methods:
1. response.json()
2. response.send()
3. response.status()
4. response.sendFiles()


# Revision:

-how to install express
-all express commands
-package.json vs package-lock.json
-dependencies  vs devdependencies 
-http requests (4)
-common response method
-Routes , Middleware (when do we use this).
-query parameters (Mostly asked)
    Ex. user.query.fullname


# Templete engiene notes:
==> notes + concepts.

Templete Engiene allow developers to create dynamic html page using data from the server.
Instead of static html, page are created dynamically.
popular templete engienes:

1. EJS (Embedded javascript)
2. PUG 
3. Handlebars

q. Why templete engienes are used?
-> They help in dynamic content.
-> Code reusability, seperate content from html.
-> because we need html to run js code.

command:
-npm install EJS
-js logic (server.js)

# activities:
-Templete engiene installation(ejs)
-Addition concepts- Q. what is static file?
                    Q. What is express router?
                    Q. Complete express application flow?

Backend project using express in 3rd project
WT folder structure